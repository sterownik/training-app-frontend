import { TextCutterPipe } from './text-cutter-pipe';

describe('TextCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
